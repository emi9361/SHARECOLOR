<% if user_signed_in? %>
	<div class="search-form" style="margin-top: 70px; text-align: center;">
	<%= form_tag(search_sound_path, :method => 'get') do %>
    <%= text_field_tag :search,'', id:'suggest' %>
    <%= submit_tag 'Search' %>
	<% end %>
<% end %>
