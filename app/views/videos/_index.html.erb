<% videos.each do |video|%>
	<tr>
		<td><%= video.user.name %></td>
		<td><%= link_to video.title, video_path(video) %></td>
		<td><%= video_tag video.file,  class:"video-sample", :controls => true %></td>
	</tr>
<% end %>