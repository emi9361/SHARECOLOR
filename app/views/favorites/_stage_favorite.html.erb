<% if user_signed_in? %>
	<% if stage.favorited_by?(current_user) %>
		<%= link_to stage_favorites_path(stage), method: :delete ,remote: true do %>お気に入り
			<i class="fas fa-bookmark" aria-hidden="true" style="color: blue;"></i>
		<% end %>
<% else %>
		<%= link_to stage_favorites_path(stage), method: :post, remote: true  do %>お気に入り
			<i class="far fa-bookmark" aria-hidden="true" ></i>
		<% end %>
	<% end %>
<% end %>
